public class Node {
    private String type;  // "operator" for AND/OR, "operand" for conditions
    private Node left;
    private Node right;
    private Object value;  // For operand nodes (e.g., age, salary)

    // Constructors, getters, setters
}

and;
@RestController
@RequestMapping("/api/rules")
